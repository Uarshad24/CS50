#include <stdio.h>
#include <cs50.h>
#include <string.h>

int main(void)
{
    string argv[5];
    int b = strlen(argv[1]);
    printf("%b\n",b);

}
if (argv[1][i] >= 'a' && argv[1][i] <= 'z')
                    {
                        argv[1][i] = argv[1][i] - 32;
                    }
